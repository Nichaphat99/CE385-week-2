//ฟังก์ชัน toGrade(score) ตรวจสอบคะแนนไม่ถูกต้อง
ถ้าคะแนน น้อยกว่า 0 หรือ มากกว่า 100 || หมายถึง หรือ จะคืนข้อความว่า "คะแนนไม่ถูกต้อง ต้องอยู่ระหว่าง 0-100" ดังนั้น -5 และ 120 จะไม่ถูกนำไปตัดเกรด

//การตัดเกรด
โปรแกรมตรวจสอบคะแนนจากมากไปน้อย
จากบนลงล่าง และเมื่อเจอเงื่อนไขที่เป็นจริงแล้ว จะหยุดตรวจสอบเงื่อนไขถัดไป

//ตัวแปร scores
สร้าง Array ที่เก็บคะแนนหลายค่า เพื่อทดสอบฟังก์ชัน

//วนลูปด้วย for...of
for...of จะหยิบคะแนนออกมาทีละตัวแล้วนำแต่ละคะแนนไปใส่ใน

//ผลลัพธ์ที่ได้
คะแนน 95 เกรด A
คะแนน 80 เกรด A
คะแนน 79 เกรด B+
คะแนน 75 เกรด B+
คะแนน 70 เกรด B
คะแนน 65 เกรด C+
คะแนน 60 เกรด C
คะแนน 55 เกรด D+
คะแนน 50 เกรด D
คะแนน 49 เกรด F
คะแนน 0 เกรด F
คะแนน -5 เกรด คะแนนไม่ถูกต้อง ต้องอยู่ระหว่าง 0-100
คะแนน 120 เกรด คะแนนไม่ถูกต้อง ต้องอยู่ระหว่าง 0-100