//ฟังก์ชัน getMenuPrice(menu)
ฟังก์ชันนี้ใช้หา ราคาพื้นฐานของเมนู switch จะนำค่าที่ส่งเข้ามาใน menu ไปเปรียบเทียบกับแต่ละ case เช่น ต้มยำกุ้ง	120 บาท

case "ข้าวมันไก่":
case "ข้าวหมูแดง":
case "ข้าวผัด":
  return 50; หมายความว่า ทั้ง 3 เมนูใช้ราคา 50 บาทเหมือนกัน

  ถ้าเมนูไม่ตรงกับ case ใดเลย จะไปที่
  default:
  return 0;
เช่น "ชาเขียว" ไม่มีอยู่ในรายการอาหาร ดังนั้นราคาจะเป็น 0

//ฟังก์ชัน getSizeMultiplier(size)
ฟังก์ชันนี้ไม่ได้คืน ราคา แต่คืน ตัวคูณราคา ตามขนาด
ธรรมดา	× 1
พิเศษ	× 1.5
จัมโบ้	× 2

เช่น ถ้าผัดไทยราคา 60 บาท:
ธรรมดา → 60 × 1 = 60
พิเศษ → 60 × 1.5 = 90
จัมโบ้ → 60 × 2 = 120

//ข้อมูล orders
รายการอาหารที่ลูกค้าสั่ง แต่ละรายการมี 3 ข้อมูล:
menu = ชื่ออาหาร
size = ขนาด
qty = จำนวน

//ตัวแปร grandTotal
สร้างตัวแปรไว้เก็บ ราคารวมทั้งบิล เริ่มต้นที่ 0 แล้วค่อย ๆ บวกราคาของแต่ละรายการเข้าไป

//วนดูรายการอาหาร
for...of จะหยิบข้อมูลจาก orders ทีละรายการมาเก็บไว้ในตัวแปร item เช่น รอบแรก: item = { menu: "ผัดไทย", size: "พิเศษ", qty: 2 }รอบต่อไปก็เป็นข้าวมันไก่ เป็นต้น

//หาราคาพื้นฐาน
นำชื่อเมนูไปให้ getMenuPrice() หา
เช่น  getMenuPrice("ผัดไทย") จะได้ 60

//หาตัวคูณขนาด
นำขนาดไปให้ getSizeMultiplier() หา
เช่น getSizeMultiplier("พิเศษ") จะได้ 1.5

//คำนวณราคาของรายการนั้น
สูตรคือ
ราคาต่อรายการ = ราคาพื้นฐาน × ตัวคูณขนาด × จำนวน
เช่น ผัดไทย: 60 × 1.5 ×  2 = 180 บาท

//บวกเข้ายอดรวม
grandTotal += itemTotal; = 180 + 50 = 230

//แสดงราคาของแต่ละรายการ
ใช้ Template Literal ด้วยเครื่องหมาย backtick ` ทำให้สามารถใส่ตัวแปรด้วย ${...}
จะได้ ผัดไทย (พิเศษ) x2 = 180 บาท

//คำนวณแต่ละรายการ
เช่น ข้าวมันไก่ 50 × 1 × 1 = 50

//ผลลัพธ์
รวมราคาบิลทั้งหมด ได้ 695 บาท 