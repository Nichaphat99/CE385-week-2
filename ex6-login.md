//ฟังก์ชัน login()
ฟังก์ชันรับข้อมูล 5 ตัว
inputUser = ชื่อผู้ใช้ที่กรอกเข้ามา
inputPass = รหัสผ่านที่กรอกเข้ามา
role = สิทธิ์ของผู้ใช้
isActive = บัญชียังใช้งานอยู่หรือไม่
age = อายุของผู้ใช้

//กำหนดข้อมูลที่ถูกต้อง
กำหนดชื่อผู้ใช้และรหัสผ่านที่ถูกต้องไว้ในตัวแปร

//ตรวจสอบชื่อผู้ใช้และรหัสผ่าน
ตรงนี้เป็นเงื่อนไขสำคัญ
!== หมายถึง ไม่เท่ากัน
|| หมายถึง หรือ
ดังนั้นเงื่อนไขนี้แปลว่า ถ้าชื่อผู้ใช้ผิด หรือ รหัสผ่านผิด ให้หยุดการทำงานและแจ้งว่าเข้าสู่ระบบไม่ได้

//ตรวจสอบว่าบัญชีถูกระงับหรือไม่
ถ้า isActive เป็น false หมายความว่าบัญชีถูกปิดหรือระงับ

//ตรวจสอบอายุ
ถ้าอายุน้อยกว่า 18 ปี จะไม่อนุญาตให้เข้าสู่ระบบ

//ตรวจสอบสิทธิ์ "อาจารย์"
ถ้า role เป็น "อาจารย์" จะเข้าสู่ระบบสำเร็จและได้รับ สิทธิ์ผู้ดูแล

//ตรวจสอบสิทธิ์ "นักศึกษา"
ถ้า role เป็น "นักศึกษา" จะเข้าสู่ระบบสำเร็จ แต่เป็น สิทธิ์ทั่วไป

//ถ้า Role ไม่ตรงกับที่กำหนด
ถ้าผ่านทุกเงื่อนไขก่อนหน้า แต่ role ไม่ใช่ทั้ง "อาจารย์" และ "นักศึกษา" ก็จะมาถึง return นี้ จะได้ สิทธิ์การใช้งานไม่ถูกต้อง